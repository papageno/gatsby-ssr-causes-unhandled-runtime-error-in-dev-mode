{
  "name": "gatsby-ssr-causes-unhandled-runtime-error-in-dev-mode",
  "version": "1.0.0",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop --host=0.0.0.0",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "start": "npm run develop",
    "serve": "gatsby serve --host=0.0.0.0",
    "clean": "gatsby clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "gatsby": "^3.0.4",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
